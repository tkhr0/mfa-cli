bin.name = "mfa-cli"
args = ["profile", "list", "--help"]

stdout = """
mfa-cli-profile-list 
Show registered profile list

USAGE:
    mfa-cli profile list

OPTIONS:
    -h, --help    Print help information
"""
